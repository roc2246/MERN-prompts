# MERN AI Instructions

Use `.agents/skills/` as the workflow source. Select the narrowest matching skill and load only its `SKILL.md`; use referenced checklists/standards only when required. Inspect only enough surrounding code to verify behavior. For edits, preserve unrelated code and run relevant existing checks.
