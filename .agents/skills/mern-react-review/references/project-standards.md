# Project Standards

Keep components focused, hooks valid and dependency-safe, state minimal, effects purposeful, and interactions accessible. Avoid speculative performance optimization. Tests should cover behavior rather than implementation details.
