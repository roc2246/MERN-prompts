# Deep Checklist

Check invalid hook/effect dependencies; stale closures; derived/duplicated state; mutation; keys; controlled inputs; async race/cancellation issues; error/loading/empty states; component coupling; prop/type design; semantic HTML/keyboard access; render hotspots. Recommend memoization only for plausible/measured cost.
