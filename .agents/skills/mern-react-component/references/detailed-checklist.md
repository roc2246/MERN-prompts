# Deep Checklist

Keep responsibilities narrow; define minimal typed props; prefer composition; use semantic HTML; preserve controlled/uncontrolled behavior; avoid derived state and unnecessary effects; handle loading/error/empty states where relevant; keep data fetching/state ownership intentional; add/update behavior-focused tests using the repo framework.
