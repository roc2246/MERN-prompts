# Project Standards

Prefer focused components with clear props, accessible markup, predictable state, and colocated tests/styles following the project structure. Add memoization or abstractions only for a concrete reason. When TypeScript exists, avoid unnecessary any/assertions.
