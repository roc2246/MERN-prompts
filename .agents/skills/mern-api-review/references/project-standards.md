# Project Standards

Express routes should be thin; controllers orchestrate request/response; business logic belongs in services. Validate and authorize at runtime boundaries. Use consistent HTTP semantics and centralized error handling. Do not expose secrets or internal error details.
