# Deep Checklist

Check selector depth/specificity, duplicated declarations, token consistency, breakpoint strategy, mobile-first flow, component boundaries, cascade/order dependencies, dead styles, and repeated calculations/patterns. Use variables/maps/mixins/functions when they encode reusable intent—not merely to make CSS look like SCSS. Avoid `@extend` coupling and overabstracted 7-1 structure.
