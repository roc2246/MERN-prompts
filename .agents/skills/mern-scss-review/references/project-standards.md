# Project Standards

Preserve rendered appearance unless change is requested. Prefer Sass modules (`@use`/`@forward`), variables/maps for shared design values, mixins/functions only when they remove meaningful repetition or encode reusable logic, shallow nesting, and component/page ownership consistent with the project architecture.
