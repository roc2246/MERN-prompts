# Deep Checklist

Cross-check docs against package scripts, env usage, routes, config, directory structure, database setup, build/start/test commands, auth flows, and deployment assumptions. Remove stale claims and undocumented required steps. Prefer concise task-oriented docs over duplicated code commentary.
