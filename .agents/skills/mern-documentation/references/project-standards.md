# Project Standards

Documentation must describe the repository as it exists, not an idealized version. Verify setup commands, scripts, environment variables, architecture, APIs, and file paths against source/configuration before changing docs.
