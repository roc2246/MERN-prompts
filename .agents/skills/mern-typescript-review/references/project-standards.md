# Project Standards

Prefer strict, accurate types over assertions and any. Distinguish compile-time contracts from runtime validation. Model domain states clearly, keep shared types intentionally shared, and avoid type-level cleverness that reduces maintainability. Align Mongoose schemas, API DTOs, and frontend contracts deliberately.
