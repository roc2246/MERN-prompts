# Claude Repository Instructions

Reusable AI behavior lives under `.agents/skills/`.

Discover the most specific matching skill, load its `SKILL.md`, and progressively load only referenced material needed for the current task. Avoid loading unrelated skills or `.agents/docs/` into context.
