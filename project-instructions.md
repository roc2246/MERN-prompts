# Project Instructions

Use the narrowest matching skill in `.agents/skills/`. Scope work to the user's request, inspect dependencies needed to verify behavior, and prefer small high-confidence changes. Never treat static types as runtime validation. When editing, preserve unrelated code, use existing conventions/dependencies, run relevant checks, and report results.
