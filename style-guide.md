# UI / SCSS Defaults

Use semantic accessible markup, mobile-first responsive layout, shallow component-oriented selectors, reusable design tokens, and consistent spacing/type scales. Use Grid/Flexbox by layout need. Add SCSS abstractions only when they reduce duplication or encode a reusable rule. Preserve established product styling unless asked to redesign.
